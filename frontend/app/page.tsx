import { Shell } from "@/components/shell";

export default function HomePage() {
  return (
    <Shell title="Immersive Label Platform">
      <p className="text-white/80">
        Exclusive catalog, artist universes, interactive releases, and premium dashboards.
      </p>
    </Shell>
  );
}
