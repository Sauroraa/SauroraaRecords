import { Shell } from "@/components/shell";

export default function LoginPage() {
  return (
    <Shell title="Login">
      <p className="text-white/80">JWT authentication entrypoint for clients, artists, and admins.</p>
    </Shell>
  );
}
