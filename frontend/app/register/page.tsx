import { Shell } from "@/components/shell";

export default function RegisterPage() {
  return (
    <Shell title="Register">
      <p className="text-white/80">Secure sign-up with anti-spam, verification and role onboarding.</p>
    </Shell>
  );
}
